import { theme } from '@/styles/theme';
import styled from 'styled-components';

export const Hr = styled.hr`
  border: none;
  border-top: 1px solid ${theme.colors.greenIconDark};
  margin: 24px 0;
`;
